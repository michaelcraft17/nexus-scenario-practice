# Optional. Leave empty for local dev -- Vite's dev server proxies /api to
# the Express server (see vite.config.js) so no base URL is needed.
# Set this only for a split production deploy where the client and server
# are on different domains, e.g. https://includai-api.up.railway.app
VITE_API_BASE_URL=
